# 2026-09-18

## Web 工作台 UI 重做（M3 Expressive，桌面 + 手机适配）

- `web/dist/app.css` 全量重写：M3 色彩/形状/海拔/弹性动效令牌，浅深双主题，氛围渐变底色。
- `web/dist/index.html` 结构对齐 M3 标准组件命名（用户要求不自定义组件名，为后续对接 E:\m3e-canvas 铺路）：top-app-bar / card / filled-button / tonal-button / icon-button / choice-chip / segmented-button / linear-progress / snackbar / drop-zone / subpanel。
- `web/dist/app.js` 仅改一处类名引用（.seg-btn → .segmented-button__option），全部元素 ID 保留，行为不变。
- 响应式：≥900px 双列（左控制 330-430px + 右画布），≤900px 单列，≤520px 按钮全宽、safe-area inset、触控目标 ≥48px、色板 pointer:coarse 放大到 42px。
- 验证：node --check 通过；playwright-cli（msedge，需 node 直跑 D:/nodejs/node_global/node_modules/@playwright/cli/playwright-cli.js，bash shim 损坏）截图 1440×900 浅色/深色 + 390×844 均正常；控制台仅 /health 未连后端的预期报错。
- 遗留：验证用的 3 张临时截图（desktop-light/mobile-light/desktop-dark.png）在仓库根目录，沙箱禁止删除，待用户手动删。

## 第二轮：杂符修复 + APP 界面 + 新图标

- 页面多余 `">` 根因：favicon data URI 内裸 `<`/`>` 未编码，预览器二次解析泄漏；已全量 URL-encode 修复（index.html + app.html）。
- 新图标：青色渐变圆角方 + 白色对话框 + 红黄蓝三彩点；favicon（编码 data URI）与顶栏 brand-mark 内联 SVG 同款，app.css 的 .brand-mark 改为容器式（去 CSS 渐变动画）。
- 新增 APP 界面：web/dist/app.html + app-shell.css。桌面(≥901px) navigation-rail（brand + FAB + 4 目的地），手机(≤900px) 底部 navigation-bar；四个屏：首页(hero + list)、上色(同源 iframe 内嵌 index.html，load 时隐藏其 top-app-bar/footer)、图库(media-grid 占位)、设置(M3 switch + list 示例，不持久化)。全部 M3 标准组件名。
- 踩坑：媒体查询 display:none 写在组件定义之前会被同优先级后置 display:flex 覆盖（rail/bar 双双失效）→ 规则必须放文件末尾。
- playwright-cli 截图归档到 out/web-preview/（PowerShell Move-Item 成功；bash mv/删除被沙箱挡）。


- 修复 colorizer_service INVALID_PROTOBUF：v6_generator.onnx 内容损坏（大小一致但 SHA256 不符，9838a8.. ≠ 48284f..），从 hf-mirror 重下并校验（48284fcf..）后替换，onnxruntime 加载验证 LOAD_OK（输入 L_bw/sam_level0/sam_level1/wd14_embedding）。v6_sam_encoder.onnx 哈希正常未动。
- 修复 web/dist/app.js 提示点双击删除 bug：dblclick 的两次 click 冒泡到 hint-layer 落点监听器导致删除后又加回新点；落点监听器增加 .target.closest('.hint-dot') || e.detail > 1 守卫（L175），node --check 通过。

- 先提交 c4672d4（APP 壳 + 双击删除修复）再改。
- 修复切回原图提示点偏移：app.js renderHints 加 clientWidth 守卫（隐藏时跳过），setMode 与 compareSeg 切回 original 后显式 renderHints() 重摆。
- 修复提示点只上色一小块：service.py 重写 _hint_colorize —— 旧实现是 64px 局部窗口亮度相似扩散；新实现 _hint_region_mask = cv2.floodFill(FLOODFILL_FIXED_RANGE, tol=22) 沿线稿泛洪 + distanceTransform 扩张半径 128px 内亮度相近(|Δ|<=22)像素并入（跨细线覆盖整头头发），GaussianBlur 羽化；返回 (hinted, coverage)，_process_hints 按 coverage 在 Lab a/b 上线性混合 auto 与 hinted（顺带删除原 placeholder 死代码行）。
- 合成图冒烟测试通过：种子区/跨 2-4px 发丝线同调区覆盖 1.00，脸区/背景/跨 50px 脸区的另一块头发零泄漏（不同物体不跟色是设计行为）。
- 用户要求回退：已 git restore 丢弃上一轮未提交的两项修复（提示点偏移重摆 + service.py 区域扩散），工作区回到 c4672d4。
