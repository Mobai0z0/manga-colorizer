name: manga_colorizer_cli
description: CLI tools for the manga colorizer — batch colorize, tint, sample generation, and comparison sheets.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.6.0
dependencies:
  args: ^2.5.0
  http: ^1.2.0
  image: ^4.10.1
  manga_colorizer_core:
    path: ../manga_colorizer_core
dev_dependencies:
  lints: ^5.0.0
